buildscript {
    ext.kotlin_version = '1.9.0'
    repositories {
        google()
        mavenCentral()
//        gradlePluginPortal()
//        maven { url "https://plugins.gradle.org/m2/" }
    }
    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}
allprojects {
    repositories {
        google()
        mavenCentral()
//        gradlePluginPortal()
//        maven { url "https://plugins.gradle.org/m2/" }
    }
    
//    configurations.all {
//        resolutionStrategy {
//            force "org.jetbrains.kotlin:kotlin-stdlib:1.7.1"
//            force "org.jetbrains.kotlin:kotlin-stdlib-common:1.7.1"
//            force "org.jetbrains.kotlin:kotlin-stdlib-jdk7:1.7.1"
//            force "org.jetbrains.kotlin:kotlin-stdlib-jdk8:1.7.1"
//        }
//    }
}

rootProject.buildDir = '../build'
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(':app')
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
